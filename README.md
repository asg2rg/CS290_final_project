# CS290_final_project

This repo contains a custom Gymnasium simulation environment for testing simple autonomous driving behavior.

## Project Structure

The main simulation environment is located at:

```text
main/gymnasium_env/envs/env.py
```
To visualize the current environment, run `simple_render.py`. This script uses random actions to move the car through the simulation. Use it for checking the rendering and basic environment behavior.
